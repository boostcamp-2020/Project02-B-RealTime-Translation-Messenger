subscription GetMessage($roomID: Int!, $userID: Int!) {
  newMessage(roomId: $roomID, id: $userID) {
    id
    text
    source
    createdAt
    user {
      id
      nickname
      avatar
      lang
    }
  }
}
