subscription GetMessage($roomId: Int!, $language: String!) {
  newMessage(roomId: $roomId, lang: $language) {
    id
    text
    source
    createdAt
    user {
      id
      nickname
      avatar
      lang
    }
  }
}
